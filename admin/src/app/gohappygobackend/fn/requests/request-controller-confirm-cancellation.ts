/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RequestResponseDto } from '../../models/request-response-dto';

export interface RequestControllerConfirmCancellation$Params {

/**
 * Request ID to confirm cancellation for
 */
  id: number;
}

export function requestControllerConfirmCancellation(http: HttpClient, rootUrl: string, params: RequestControllerConfirmCancellation$Params, context?: HttpContext): Observable<StrictHttpResponse<RequestResponseDto>> {
  const rb = new RequestBuilder(rootUrl, requestControllerConfirmCancellation.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RequestResponseDto>;
    })
  );
}

requestControllerConfirmCancellation.PATH = '/api/request/{id}/confirm-cancellation';
