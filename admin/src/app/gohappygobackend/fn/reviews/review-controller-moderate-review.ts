/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateReviewResponseDto } from '../../models/create-review-response-dto';
import { ModerateReviewDto } from '../../models/moderate-review-dto';

export interface ReviewControllerModerateReview$Params {
  id: number;
      body: ModerateReviewDto
}

export function reviewControllerModerateReview(http: HttpClient, rootUrl: string, params: ReviewControllerModerateReview$Params, context?: HttpContext): Observable<StrictHttpResponse<CreateReviewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewControllerModerateReview.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CreateReviewResponseDto>;
    })
  );
}

reviewControllerModerateReview.PATH = '/api/review/{id}/moderate';
