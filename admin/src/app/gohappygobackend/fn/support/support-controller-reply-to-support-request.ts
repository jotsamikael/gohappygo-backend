/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RespondSupportRequestDto } from '../../models/respond-support-request-dto';
import { SupportRequestResponseDto } from '../../models/support-request-response-dto';

export interface SupportControllerReplyToSupportRequest$Params {

/**
 * Support request ID
 */
  id: number;
      body: RespondSupportRequestDto
}

export function supportControllerReplyToSupportRequest(http: HttpClient, rootUrl: string, params: SupportControllerReplyToSupportRequest$Params, context?: HttpContext): Observable<StrictHttpResponse<SupportRequestResponseDto>> {
  const rb = new RequestBuilder(rootUrl, supportControllerReplyToSupportRequest.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SupportRequestResponseDto>;
    })
  );
}

supportControllerReplyToSupportRequest.PATH = '/api/support/{id}/reply';
