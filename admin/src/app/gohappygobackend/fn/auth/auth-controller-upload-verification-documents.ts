/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UploadVerificationResponseDto } from '../../models/upload-verification-response-dto';

export interface AuthControllerUploadVerificationDocuments$Params {
  
    /**
     * Upload verification files
     */
    body: {

/**
 * Selfie photo of the user
 */
'selfie': Blob;

/**
 * Front side of national ID
 */
'idFront': Blob;

/**
 * Back side of national ID
 */
'idBack': Blob;

/**
 * Additional notes for verification
 */
'notes'?: string;
}
}

export function authControllerUploadVerificationDocuments(http: HttpClient, rootUrl: string, params: AuthControllerUploadVerificationDocuments$Params, context?: HttpContext): Observable<StrictHttpResponse<UploadVerificationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, authControllerUploadVerificationDocuments.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UploadVerificationResponseDto>;
    })
  );
}

authControllerUploadVerificationDocuments.PATH = '/api/auth/upload-verification';
