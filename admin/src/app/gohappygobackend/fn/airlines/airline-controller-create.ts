/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AirlineControllerCreate$Params {
      body: {

/**
 * ICAO code (3-4 letters)
 */
'icaoCode': string;

/**
 * IATA code (2 letters)
 */
'iataCode'?: string;

/**
 * Airline name
 */
'name': string;

/**
 * Airline prefix
 */
'prefix'?: string;

/**
 * Fleet size
 */
'fleetSize'?: number;

/**
 * Number of destinations
 */
'destinationsCount'?: number;

/**
 * Callsign
 */
'callsign'?: string;

/**
 * Wikipedia URL
 */
'wikipediaUrl'?: string;

/**
 * Airline logo image file
 */
'logo'?: Blob;
}
}

export function airlineControllerCreate(http: HttpClient, rootUrl: string, params: AirlineControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, airlineControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

airlineControllerCreate.PATH = '/api/airline';
