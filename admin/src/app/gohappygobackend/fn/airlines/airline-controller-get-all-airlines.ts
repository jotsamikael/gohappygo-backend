/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedAirlinesResponseDto } from '../../models/paginated-airlines-response-dto';

export interface AirlineControllerGetAllAirlines$Params {

/**
 * Page number for pagination
 */
  page?: number;

/**
 * Number of items per page
 */
  limit?: number;

/**
 * Search by airline name
 */
  name?: string;

/**
 * Search by IATA code
 */
  iataCode?: string;

/**
 * Search by ICAO code
 */
  icaoCode?: string;

/**
 * Search by callsign
 */
  callsign?: string;

/**
 * Sort order
 */
  orderBy?: 'name:asc' | 'name:desc' | 'iataCode:asc' | 'iataCode:desc' | 'icaoCode:asc' | 'icaoCode:desc' | 'createdAt:asc' | 'createdAt:desc';
}

export function airlineControllerGetAllAirlines(http: HttpClient, rootUrl: string, params?: AirlineControllerGetAllAirlines$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedAirlinesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, airlineControllerGetAllAirlines.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('name', params.name, {});
    rb.query('iataCode', params.iataCode, {});
    rb.query('icaoCode', params.icaoCode, {});
    rb.query('callsign', params.callsign, {});
    rb.query('orderBy', params.orderBy, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedAirlinesResponseDto>;
    })
  );
}

airlineControllerGetAllAirlines.PATH = '/api/airline';
